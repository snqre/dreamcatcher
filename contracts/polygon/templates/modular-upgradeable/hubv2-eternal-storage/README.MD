An eternal storage compatible version of the Hub, although now its just an implementation which makes it upgradeable but we can do this without losing the data

it is vastly more lite weight that the previous mostly because storage is being outsourced

Validator > Role > Timelock