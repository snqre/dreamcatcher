# dreamcatcher


```typescript
"""
/** @inteface - This `type` is intended to be used as an `interface`. */
/**
 * 
 * Class Guidelines
 * 
 * Guidelines
 * 
 * @interface > This is intended to be used as an interface.
 * @class > This is intended to be used as a class.
 * @public > This is an exposed method.
 * @reference >  This method or function returns a reference to something.
 * @reference ? > This method or function may or may not return a reference to something.
 * @new > This method or function returns a completly new reference to something and no other part of the code has a reference of it. It is completely brand new.
 * @private > This is indended to be used within a class.
 * @copy > This is a brand new copy of something.
 * @state > This is where the state of a class is located.
 * @constructor > The constructor block.
 * @impl > Is intended to follow the blueprint of a given interface or type.
*/
"""